name: cicd tests

on:
  push:
    branches:
      - main
    # tags: [ tmp-rerun ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: build and test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run tests
      run: make tests
